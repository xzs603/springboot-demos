spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/springboot_jpa
    username: root
    password: root

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update

  redis:
    host: localhost
# Redis服务器连接端口
    port: 6379

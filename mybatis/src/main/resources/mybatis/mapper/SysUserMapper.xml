<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mttsui.mybatis.mapper.SysUserMapper">

  <select id="query1" resultType="com.mttsui.mybatis.entity.SysUser">
    SELECT id, name, password, act_ind, user_name, birthday, nick_name FROM t_sys_user
  </select>

  <select id="query2" resultType="com.mttsui.mybatis.entity.SysUser">
    SELECT id, name, password, act_ind actInd, user_name userName, birthday, nick_name FROM t_sys_user
  </select>

  <select id="query3" resultType="map">
    SELECT id, name, password, act_ind, user_name, birthday, nick_name FROM t_sys_user
  </select>
</mapper>